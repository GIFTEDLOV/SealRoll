Commit and push all Phase 3 polish work in one commit.

1. From C:\dev, run:
   git add -A
2. Run: git status
   Show me the output. I want to verify what's staged before committing.
3. If the staged file list looks clean (no .env.local, no github-token.txt, no logo.png.png), proceed:
   git commit --no-verify -m "Phase 3A: Sealroll branding, dark theme, landing page, transaction toasts"
   git push
4. Show me full output of every command.

Stop after the push.